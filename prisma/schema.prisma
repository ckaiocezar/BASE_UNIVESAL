// prisma/schema.prisma
datasource db {
  provider = "sqlite"  // ou "postgresql" se usar PostgreSQL
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
}
